rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /kirt/{document=**} {
      allow read, write: if true;
      // Kirt-only personal app. No auth needed.
      // Data is not sensitive (already in Apple Health).
    }
  }
}
